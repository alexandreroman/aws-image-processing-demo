# Sample images

Drop 20 JPG files named `1.jpg` through `20.jpg` here. They are
uploaded to the demo S3 bucket under `samples/` by
`scripts/upload-samples.sh`.

These are the images the frontend picks from at random when the user
clicks **Start burst** on the landing page.
